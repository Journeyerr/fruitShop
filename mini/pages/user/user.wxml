<!--pages/user/user.wxml-->
<view class="container">
  <!-- 用户信息卡片 -->
  <view class="user-card">
    <!-- 未登录状态 -->
    <view class="user-info" wx:if="{{!isLogin}}">
      <view class="user-content">
        <button class="login-btn" bindtap="getUserProfile">
          <text>微信登录</text>
        </button>
        <text class="login-tip">登录后享受更多权益</text>
      </view>
    </view>
    
    <!-- 已登录状态 -->
    <view class="user-info" wx:else>
      <image class="user-avatar" src="{{userInfo.avatar}}" mode="aspectFill"></image>
      <view class="user-content">
        <text class="user-name">{{userInfo.nickname}}</text>
        <text class="user-phone">{{userInfo.phone || '未绑定手机'}}</text>
      </view>
    </view>
  </view>

  <!-- 订单快捷入口 -->
  <view class="order-card card">
    <view class="order-header">
      <text class="order-title">我的订单</text>
      <view class="order-more" bindtap="viewOrders">
        <text>全部订单</text>
        <text class="arrow">›</text>
      </view>
    </view>
    <view class="order-icons">
      <view class="order-icon-item" bindtap="viewOrders" data-status="pending">
        <view class="icon-wrap">
          <image src="/images/order-pending.png" mode="aspectFit"></image>
          <view class="badge" wx:if="{{orderCounts.pending > 0}}">{{orderCounts.pending}}</view>
        </view>
        <text>待付款</text>
      </view>
      <view class="order-icon-item" bindtap="viewOrders" data-status="delivering">
        <view class="icon-wrap">
          <image src="/images/order-paid.png" mode="aspectFit"></image>
          <view class="badge" wx:if="{{orderCounts.delivering > 0}}">{{orderCounts.delivering}}</view>
        </view>
        <text>待收货</text>
      </view>
      <view class="order-icon-item" bindtap="viewOrders" data-status="delivering">
        <view class="icon-wrap">
          <image src="/images/order-completed.png" mode="aspectFit"></image>
          <view class="badge" wx:if="{{orderCounts.completed > 0}}">{{orderCounts.completed}}</view>
        </view>
        <text>已完成</text>
      </view>
    </view>
  </view>

  <!-- 配送信息 -->
  <view class="delivery-card card">
    <view class="delivery-header">
      <image src="/images/delivery.png" mode="aspectFit" class="delivery-icon"></image>
      <text class="delivery-title">配送服务</text>
    </view>
    <view class="delivery-info">
      <view class="delivery-item">
        <text class="delivery-label">配送范围</text>
        <text class="delivery-value">周边5公里内</text>
      </view>
      <view class="delivery-item">
        <text class="delivery-label">配送时间</text>
        <text class="delivery-value">每日 08:00-20:00</text>
      </view>
      <view class="delivery-item">
        <text class="delivery-label">配送费</text>
        <text class="delivery-value highlight">满59元免配送费</text>
      </view>
    </view>
  </view>

  <!-- 功能菜单 -->
  <view class="menu-card card">
    <view class="menu-list">
      <view class="menu-item"
            wx:for="{{menuItems}}"
            wx:key="id"
            bindtap="{{item.id === 'service' ? 'callService' : 'onMenuTap'}}"
            data-id="{{item.id}}">
        <image class="menu-icon" src="{{item.icon}}" mode="aspectFit"></image>
        <text class="menu-name">{{item.name}}</text>
        <view class="menu-right">
          <text class="menu-badge" wx:if="{{item.badge}}">{{item.badge}}</text>
          <text class="arrow" wx:if="{{item.id !== 'service'}}">›</text>
        </view>
      </view>
    </view>

    <!-- 分割线 -->
    <view class="divider" wx:if="{{isLogin}}"></view>

    <!-- 退出登录按钮 -->
    <button class="logout-button" wx:if="{{isLogin}}" bindtap="logout">
      退出登录
    </button>
  </view>
</view>
